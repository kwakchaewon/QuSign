# QuSign 서비스 테스트 시나리오

> 현재 구현된 기능 기반 수동 테스트용 시나리오 (2026-06-13 기준)
>
> 체크 방법: 통과 `[V]` / 실패 `[X]` / 미확인 `[ ]`

## 사전 준비

- 테스트 PDF 파일 1~3개 준비
- 브라우저 2개 (또는 시크릿 창) 열기
- 서비스 기동:
  ```bash
  docker compose up -d          # MinIO + Redis
  ./gradlew bootRun             # 백엔드 (local 프로파일)
  cd frontend && npm run dev    # 프론트엔드
  ```
- 테스트 계정 3개 필요
  - **A (요청자):** `requester@test.com` / `Test1234!`
  - **B (서명자):** `signer@test.com` / `Test1234!`
  - **ADMIN:** 환경변수 `ADMIN_EMAIL` / `ADMIN_PASSWORD` 로 지정 (앱 시작 시 자동 생성)

> **SSE 단기 토큰 패치 완료 (2026-06-12):**
> `notification.ts` `connectSSE()`가 내부적으로 `POST /api/notifications/sse-token`을 호출하여
> UUID 단기 토큰을 발급받은 후 SSE 연결. JWT가 URL에 노출되지 않음.

---

## 시나리오 1 — 기본 서명 플로우

**목적:** 핵심 플로우 전체 확인

- [V] **1** [A] `/signup` → `requester@test.com` / `Test1234!` 가입 → 로그인 페이지 이동 확인
- [V] **2** [B] `/signup` → `signer@test.com` / `Test1234!` 가입
- [V] **3** [A] 로그인 → `/home` 진입 → 대시보드 통계 카드 모두 0 표시
- [V] **4** [A] `서명 요청` → PDF 업로드 → 서명자 이메일 `signer@test.com` 입력 → 요청 생성 → 완료 화면에서 서명 링크 표시
- [V] **5** [A] 헤더 벨 아이콘 → 배지 없음 확인 (아직 서명 전)
- [V] **6** [B] 로그인 → 헤더 벨 아이콘 → 🔔 미읽음 1 "서명 요청" 알림 확인
- [V] **7** [B] 알림 클릭 → `/sign/{token}` 서명자 화면 진입 (미로그인 시 로그인 후 자동 이동)
- [V] **8** [B] 문서 정보(요청자·만료일·SHA3-256) 확인 → PDF 미리보기 스크롤 → 동의 체크박스 2개 체크 → 계정 비밀번호 입력 → 서명하기 → "서명 완료" 화면
- [V] **9** [A] 헤더 벨 아이콘 → 🔔 미읽음 1 "서명 완료" 알림 확인
- [V] **10** [A] 알림 클릭 → 문서 상세 페이지 → 서명자 상태 `SIGNED` + 서명 일시 표시
- [V] **11** [A] 서명된 PDF 다운로드 → `파일명_qusigned.pdf` 다운로드 확인
- [V] **12** [A] `/verify` → 토큰 입력 → ✅ 유효한 서명 + 서명자 이메일 표시
- [V] **13** [A] `/verify` → 서명된 PDF 파일 업로드 → 동일한 검증 결과
- [ ] **14** [A] 문서 상세 페이지 하단 감사 로그 타임라인 → `SIGN_REQUEST_CREATED` → `SIGNED` → `SIGNED_DOCUMENT_DOWNLOADED` 이벤트 순서 확인

---

## 시나리오 2 — 번들 서명 (다중 문서)

**목적:** 여러 PDF를 번들로 묶어 한 번의 서명으로 전체 처리

- [V] **1** [A] 로그인 → `서명 요청` 진입
- [V] **2** PDF 3개 드래그앤드롭 업로드 → 파일 목록 3행 표시, 각 파일 해시값 계산 확인
- [V] **3** 서명자 이메일 pill 방식으로 입력 (`signer@test.com` + Enter) → pill 표시
- [V] **4** 요청 생성 → 완료 화면에서 **"파일명.pdf 외 2건"** 번들 표시 + 서명자별 링크 **1개** 생성 확인 (문서별 3개 아님)
- [ ] **5** [A] 대시보드(`/documents`) 진입 → 목록에 **"파일명.pdf 외 2건"** + 숫자 뱃지 `3` 표시, 개별 문서 행 미표시 확인
- [ ] **6** [A] 번들 행 클릭 → `/bundles/{id}` 번들 상세 페이지 진입 → 3개 문서 파일명 목록 확인
- [V] **7** [B] 로그인 → 알림 확인 → **알림 1개** ("파일명.pdf 외 2건 서명 요청")
- [V] **8** [B] 알림 클릭 → `/sign/{bundle_token}` 서명자 화면 진입 → 문서명 **"파일명.pdf 외 2건"** + 파일 목록 3개 표시 확인
- [V] **9** [B] 동의 체크박스 2개 체크 → 비밀번호 입력 → 서명하기 → 3개 문서 **전부** 서명 처리됨
- [V] **10** [B] 서명 완료 화면에서 문서별 다운로드 버튼 3개 확인 → 각각 클릭 시 `파일명_qusigned.pdf` 개별 다운로드
- [ ] **11** [A] 번들 상세 페이지 새로고침 → 서명자 상태 `SIGNED` + 서명 일시 표시 + 문서별 다운로드 버튼 3개 확인
- [V] **12** PDF 5개 초과 업로드 시도 → 클라이언트 차단 메시지 표시
- [ ] **13** [A] 번들 상세 페이지 하단 감사 로그 타임라인 → `BUNDLE_REQUEST_CREATED` → `BUNDLE_SIGNED` 이벤트 확인

---

## 시나리오 3 — 서명 취소 플로우

**목적:** 요청자가 PENDING 서명 취소, 서명자에게 알림

- [ ] **1** [A] 서명 요청 생성 (서명자: `signer@test.com`)
- [ ] **2** [A] 문서 상세 페이지 → 서명자 행의 "취소" 버튼 클릭 → 확인 모달 표시
- [ ] **3** 모달 확인 → 서명자 상태 `CANCELLED`로 변경 확인
- [ ] **4** [B] 알림 확인 → ❌ "서명 요청 취소" 알림
- [ ] **5** [B] 서명 링크 직접 접속 → "취소된 요청" 안내 화면
- [ ] **6** [A] CANCELLED 상태 서명자 행 → 취소 버튼 미표시 확인
- [ ] **7** [A] 감사 로그 타임라인 → `SIGNER_CANCELLED` 이벤트 기록 확인

---

## 시나리오 4 — 만료 토큰 처리

**목적:** 만료된 서명 링크 접근 동작 확인

- [ ] **1** MariaDB에서 `expires_at`을 과거로 직접 수정
  ```sql
  -- 접속: mariadb -u root -proot qusign
  -- 가장 최근 PENDING 서명 요청 만료 처리
  UPDATE signature_requests
  SET expires_at = '2020-01-01 00:00:00'
  WHERE signer_email = 'signer@test.com'
    AND status = 'PENDING'
  ORDER BY created_at DESC
  LIMIT 1;
  -- 확인
  SELECT id, token, signer_email, expires_at, status FROM signature_requests ORDER BY created_at DESC LIMIT 5;
  ```
- [ ] **2** 만료된 링크로 `/sign/{token}` 접속 → "만료된 요청" 안내 화면
- [ ] **3** 상세 페이지에서 해당 서명자 상태 → `EXPIRED` 배지 표시

---

## 시나리오 5 — 계정 설정

**목적:** 비밀번호 변경, 알림 설정, 탈퇴

- [ ] **1** [A] 로그인 → `/settings` → 비밀번호 변경 / 알림 / 탈퇴 섹션 표시
- [V] **2** 비밀번호 변경: 현재 `Test1234!` → 새 `NewPass456!` → 성공 토스트
- [V] **3** 로그아웃 후 기존 비밀번호로 로그인 시도 → 로그인 실패
- [V] **4** 새 비밀번호로 로그인 → 성공
- [ ] **5** 알림 설정에서 "서명 완료 알림" OFF → 저장 성공
- [ ] **6** 다른 계정이 서명 완료 후 [A] 알림 확인 → 알림 미생성
- [ ] **7** 계정 탈퇴: 이메일 입력 확인 → 탈퇴 → 로그인 화면으로 이동
- [ ] **8** 탈퇴한 이메일로 로그인 시도 → "탈퇴한 계정" 오류
- [ ] **9** 탈퇴 전 발급된 JWT로 API 직접 호출 (`Authorization: Bearer <token>`) → 401 반환 확인 (**A01 보안 패치 검증**)

---

## 시나리오 6 — 엣지 케이스

- [ ] **중복 서명 요청** — 동일 문서 + 동일 서명자로 PENDING 중 재요청 → 409 "이미 요청 중" 오류
- [ ] **잘못된 비밀번호로 서명** — 서명 실행 시 틀린 비밀번호 입력 → 400 "비밀번호 오류"
- [ ] **타인 문서 다운로드** — [B]가 [A]의 문서 ID로 다운로드 시도 → 403/404
- [ ] **50MB 초과 PDF 업로드** — 대용량 파일 업로드 시도 → 클라이언트 사전 차단
- [ ] **PDF 아닌 파일 업로드** — `.jpg` 파일 업로드 시도 → 파일 형식 오류
- [ ] **비밀번호 규칙 위반 가입** — `1234` 입력 → 실시간 규칙 피드백 + 차단

---

## 시나리오 7 — 감사 로그 (Audit Trail)

**목적:** 법적 증거 수준 감사 이벤트 기록·조회 확인 (4-8)

- [ ] **1** [A] 서명 요청 생성 → 문서 상세 페이지 하단 **감사 로그 타임라인** 확인
  - `SIGN_REQUEST_CREATED` 이벤트, 요청자 이메일, 타임스탬프 표시
- [ ] **2** [B] 서명 완료 후 타임라인 새로고침 → `SIGNED` 이벤트 추가 확인
- [ ] **3** [A] 서명된 PDF 다운로드 → 타임라인에 `SIGNED_DOCUMENT_DOWNLOADED` 기록 확인
- [ ] **4** [A] 상세 페이지 → "감사 로그 내보내기" 버튼 클릭 → JSON 파일 다운로드 → 이벤트 배열 포함 확인
- [ ] **5** 번들 서명 완료 후 `/bundles/{id}` 상세 페이지 감사 로그 타임라인 → `BUNDLE_REQUEST_CREATED`, `BUNDLE_SIGNED` 확인
- [ ] **6** API 직접 호출: `GET /api/documents/{id}/audit` → `userAgent` 필드 미포함 확인 (내부 기록 전용)
- [ ] **7** 감사 로그 DB 직접 확인: `DELETE FROM audit_logs` 실행 시 권한 오류 또는 서비스 단에서 삭제 메서드 미존재 확인 (불변성)

---

## 시나리오 8 — 관리자 페이지

**목적:** ADMIN 계정의 시스템 전체 조회·관리 기능 및 접근 제어 확인 (4-9)

### 사전 준비
- 환경변수 `ADMIN_EMAIL=admin@qusign.com` / `ADMIN_PASSWORD=Admin1234!` 설정 후 앱 재시작
- 관리자 계정 자동 생성 확인 (`AdminInitializer` 동작)

### 접근 제어
- [ ] **1** [A] 일반 계정으로 `/admin` 진입 시도 → `/home` 리다이렉트 확인
- [ ] **2** [A] JWT로 `GET /api/admin/stats` 직접 호출 → 403 반환 확인

### 관리자 기능
- [ ] **3** 관리자 계정으로 로그인 → `/admin` 진입 → 통계 카드 (총 사용자·서명 수·상태별 카운트) 표시
- [ ] **4** 사이드바 "사용자" → `/admin/users` → 전체 사용자 목록 표시, 이메일 검색 동작 확인
- [ ] **5** `signer@test.com` 검색 → 해당 사용자 행의 "비활성화" 버튼 클릭 → 확인 모달 → 처리
- [ ] **6** [B] 비활성화 후 기존 JWT로 `GET /api/signature-requests/received` 호출 → **401 반환 확인** (A01 패치)
- [ ] **7** [B] 로그인 재시도 → "비활성화된 계정" 오류 확인
- [ ] **8** 사이드바 "감사 로그" → `/admin/audit` → 전체 감사 로그 테이블 표시
- [ ] **9** 이벤트 타입 필터(`SIGNED`) + 날짜 범위 필터 → 필터링 결과 반영 확인
- [ ] **10** "전체 감사 로그 내보내기" 버튼 → JSON 다운로드 → 전체 이벤트 포함 확인
- [ ] **11** `GET /api/admin/users/{email}/audit` 직접 호출 → 해당 사용자 이벤트만 반환 확인

---

## 시나리오 9 — OWASP 보안 검증

**목적:** 5단계 보안 패치 동작 확인

### A01 — 계정 상태 즉시 차단
- [ ] **1** [A] 로그인 → JWT 토큰 복사
- [ ] **2** 관리자가 [A] 계정 비활성화 (`PUT /api/admin/users/{email}/disable`)
- [ ] **3** 복사한 JWT로 즉시 API 호출 (`GET /api/documents`) → **401 반환** (만료 전이어도 차단)
- [ ] **4** [A] 계정 탈퇴 후 동일 JWT로 API 호출 → 401 반환

### A05 — HTTP 보안 헤더
- [ ] **5** 임의 API 응답 헤더 확인 (curl 또는 브라우저 DevTools):
  - `X-Frame-Options: DENY` 포함 확인
  - `X-Content-Type-Options: nosniff` 포함 확인
  - `Referrer-Policy: no-referrer` 포함 확인

### A05 — CORS
- [ ] **6** `Origin: http://malicious.example.com` 헤더로 API 요청 → CORS 오류 (허용되지 않은 오리진 차단)

### A07 — SSE 단기 토큰

- [ ] **7** `POST /api/notifications/sse-token` (인증 필요, `Authorization: Bearer <jwt>`) → `sseToken` UUID 반환 확인
- [ ] **8** 반환된 `sseToken`으로 `GET /api/notifications/stream?token={sseToken}` 연결 → SSE 스트림 수립 확인
- [ ] **9** 동일 `sseToken` 재사용 시도 → 401 반환 (일회성 소비 확인)
- [ ] **10** 30초 후 미사용 `sseToken`으로 연결 시도 → 401 반환 (TTL 만료 확인)
- [ ] **11** 서버 접근 로그 확인 → SSE 연결 URL에 JWT 원문 미노출 (`?token=UUID` 형태만 기록)

---

## 시나리오 10 — 통합 대시보드 & 받은 문서

**목적:** 보낸·받은 요청 통합 대시보드 및 받은 문서 전용 화면 동작 확인 (계획 외 구현 완료)

- [ ] **1** [A] 로그인 → `/home` → 보낸·받은 통합 대시보드(`UnifiedDashboardView`) 진입 — 탭 또는 필터 UI 표시 확인
- [ ] **2** [A] "보낸 요청" 탭 → 본인이 생성한 서명 요청 목록 표시 확인
- [ ] **3** [B] 로그인 → `/home` "받은 요청" 탭 → 서명자로 받은 요청 목록 표시 확인
- [ ] **4** [A] 홈 화면 요약 카드 — 전체 문서 수 / 서명 완료 / 대기 중 / 만료 건수 정확히 표시 (`GET /api/dashboard/summary`)
- [ ] **5** [A] 홈 화면 "할 일" 섹션 — 서명 대기 중인 요청 목록 표시 (`GET /api/dashboard/actions`)
- [ ] **6** [B] 받은 문서 전용 화면(`ReceivedDocumentsView`) 진입 → 서명자로 받은 요청 목록 표시 확인
- [ ] **7** [B] 받은 문서 행 클릭 → 받은 문서 상세(`ReceivedDetailView`) → 요청자 정보 / 문서 정보 / 서명 상태 표시

---

## 시나리오 11 — 추가 구현 기능 검증

**목적:** 계획 외 추가 구현된 기능 동작 확인

### 서명 요청 메시지

- [ ] **1** [A] 서명 요청 생성 시 메모(message) 필드 입력 (`"계약서 검토 후 서명 부탁드립니다."`)
- [ ] **2** [B] 서명자 화면(`/sign/{token}`) → 요청자 메모 표시 확인

### 재업로드 차단

- [ ] **3** [A] 이미 서명 완료된 PDF(`파일명_qusigned.pdf`)를 새 서명 요청에 업로드 시도 → "이미 서명된 문서" 차단 메시지 표시 확인 (SHA3-256 해시 중복 검사)

### UX 안전장치

- [ ] **4** 서명 요청 페이지에서 동일 이메일을 서명자 pill로 중복 입력 시도 → 중복 추가 방지 확인
- [ ] **5** PDF 업로드 후 제출 완료 전 페이지 이탈 시도 (브라우저 뒤로가기 / 탭 닫기) → 이탈 방지 경고 표시 확인 (`beforeunload`)
- [ ] **6** 만료된 JWT로 API 요청 수신 시 → 401 자동 로그아웃 → 로그인 화면 이동 확인

### 검증 페이지 헤더 분기

- [ ] **7** `/verify` 비로그인 상태 접속 → 공통 네비게이션 헤더 없이 검증 UI만 표시
- [ ] **8** 로그인 후 `/verify` 접속 → 공통 헤더 (벨 아이콘·계정 메뉴 포함) 표시 확인

### 자리비움 자동 잠금

- [ ] **9** 로그인 후 일정 시간 미조작 → 자동 로그아웃 → 로그인 화면으로 이동 확인

---

## 알림 실시간성 확인

A/B 두 창을 나란히 열고:

- [ ] [A]가 서명 요청 생성 → [B] 화면에서 벨 아이콘 배지가 **즉시(< 1초)** 증가
- [ ] [B]가 서명 실행 → [A] 화면에서 벨 아이콘 배지가 **즉시** 증가

페이지 새로고침 없이 실시간으로 반응하면 SSE 정상 동작 확인 완료.

---

## 미구현 시나리오 (향후 추가 예정)

### 4-7 — 서명 링크 재발송 (미구현)

> `POST /api/signature-requests/{id}/resend` 구현 후 테스트 예정

- [ ] EXPIRED 상태 서명자 행 → "재발송" 버튼 표시 확인
- [ ] 재발송 클릭 → 새 토큰 발급 + 기존 토큰 무효화 확인
- [ ] 기존(만료된) 링크 접속 → "유효하지 않은 링크" 안내 화면

### 서명 거절 (미구현)

> `POST /api/signature-requests/{token}/reject` 구현 후 테스트 예정

- [ ] 서명자가 "거절" 버튼 클릭 + 사유 입력 → 요청자에게 `SIGN_REJECTED` 알림
- [ ] 상세 페이지 `REJECTED` 배지 + 사유 표시

### 알림 드롭다운 "전체 보기" 링크 (보류)

> `NotificationDropdown.vue`의 "전체 보기" 버튼이 현재 주석 처리됨.  
> Claude Design 목업 완성 후 복원 예정. 백엔드 API(`GET /api/notifications?page=N`)와 라우트(`/notifications`)는 구현되어 있음.

- [ ] 알림 드롭다운 "전체 보기" 클릭 → `/notifications` 이동 → 전체 알림 이력 (페이지네이션) 표시 확인
